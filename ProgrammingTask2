# The aim is to produce code that will check whether you have guessed the correct randomly generated number or not. If
# correct then lets you know of the correct guess otherwise if too high or low, it will keep prompting you for a guess
# until you guess the correct number.

# importing the 'random' library which we will use to generate the random integer
import random


# This function gets user's input in integer format and passes that value onto the next function (checks for validity
# of guess).
def get_user_input():
    user_guess = int(input("Enter your guess for a randomly generated number between 1 and 100: "))
    checking_validity_of_input(user_guess)


# This function checks whether user's initial guess is outside the range 1 to 100, if so print error message and call
# earlier function to re-prompt the user to enter another guess, otherwise, call the function that will generate a
# random number, then call a second function that will take the randomly generated number as well as the user's guess
# and check if they are correct.
def checking_validity_of_input(the_guess):
    if the_guess < 1 or the_guess > 100:
        print("Your guess is out of the range 1 to 100, please try again.")
        get_user_input()
    computers_number = generate_random_computer_number()
    check_guess_against_generated_number(the_guess, computers_number)


# This function generates the random number between 1 and 100 (inclusive)
def generate_random_computer_number():
    computer_number = random.randint(1, 100)
    return computer_number


# This function checks whether the user's guess is too high or too low or is the correct guess, and calls the
# appropriate function that will display the respective message.
def check_guess_against_generated_number(the_guess, computer_number):
    if the_guess > computer_number:
        guess_is_too_high(the_guess, computer_number)
    elif the_guess < computer_number:
        guess_is_too_low(the_guess, computer_number)
    else:
        guess_is_correct(the_guess, computer_number)


# This function informs user their guess is lower than the generated number, meaning they need to put another guess in,
# then we call the previous function (function just above) to check that new input's status in relation to the generated
# number (still lower, higher, or correct guess).
def guess_is_too_low(the_guess, computer_number):
    user_guess = int(input("Go HIGHER, try another number between " + str(the_guess) + " and 100: "))
    check_guess_against_generated_number(user_guess, computer_number)


# This function informs user their guess is higher than the generated number, meaning they need to put another guess in,
# then we call the previous function (function just above) to check that new input's status in relation to the generated
# number (lower, still higher, or correct guess).
def guess_is_too_high(the_guess, computer_number):
    user_guess = int(input("Go LOWER, try another number between 0 and " + str(the_guess) + ": "))
    check_guess_against_generated_number(user_guess, computer_number)


# This function informs user their guess is correct by printing a message stating their guess and the generated number
def guess_is_correct(the_guess, computer_number):
    print("Good Job! Your guess was " + str(the_guess) + " and the random number was " + str(computer_number))


# This line initiates the game by calling the function that will get the user's input (function at the very top)
get_user_input()
